# Bloom — Python CV data collection only.
# All HTTP routing is handled by the Express server (server.js).

# Computer vision
opencv-python
mediapipe
numpy

# HTTP client — pushes CV metrics to Express
requests

# Environment
python-dotenv

# Input monitoring (screen_monitor.py)
pynput
pillow

# macOS only — for active window detection
pyobjc-framework-Cocoa; sys_platform == "darwin"

# Windows only
pygetwindow; sys_platform == "win32"
